# Проект 5-го спринта 

## Автотесты для сервиса Stellar Burgers

### Технологии для написания автотестов:

- **Python:** 3.13.7
- **pytest:** 8.3.5
- **Selenium:** 4.27.1
- **Chrome Browser:** 142.0.7444.176
- **ChromeDriver:** 142.0.7444.175

### Установка проекта

Установите зависимости:

```bash
pip install -r requirements.txt
````
Комментарии:

Убрал весь текст в отдельный файл data.py
Объединил тесты и убрал лишние классы в тестах - test_account_login_page
В тестах - test_of_the_constructor_page сделал параметризацию и условные ожидания для стабильности
С чем столкнулся при выполнении проекта:

Основная сложность была при применении параметризации, слишком долго искал информацию как лучше ее применить,
ничего лучшего чем создать специально под конструктор отдельную фикстуру не нашел.
Но теперь тест - test_transition_the_constructor_click_logo стал проходить дольше
из-за каких-то фоновых сервисов Google Chrome при запуске в терминале командой:

````bash

pytest -v -s
````